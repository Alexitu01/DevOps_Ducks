prometheus.exporter.unix "local_system" { }

prometheus.scrape "scrape_metrics" {
    targets = 
    forward_to =
    scrape_interval = "10s"
}
logging{
    level = "debug"
    format = "logfmt"
}

